# SPDX-License-Identifier: GPL-2.0-or-later

# script for GigaDevice gd32f3x0 Cortex-M4 Series (GD32F330/GD32F350/GD32F355)

# https://www.gigadevice.com/microcontroller/gd32f350c8t6/

#
# gd32f3x0 devices support SWD transport only.
#
source [find target/swj-dp.tcl]
source [find mem_helper.tcl]

if { [info exists CHIPNAME] } {
   set _CHIPNAME $CHIPNAME
} else {
   set _CHIPNAME gd32f3x0
}

# Work-area is a space in RAM used for flash programming.
# Default to 4kB, safe for the smallest GD32F3x0 variants.
if { [info exists WORKAREASIZE] } {
   set _WORKAREASIZE $WORKAREASIZE
} else {
   set _WORKAREASIZE 0x1000
}

# Allow overriding the Flash bank size
if { [info exists FLASH_SIZE] } {
    set _FLASH_SIZE $FLASH_SIZE
} else {
    # autodetect size
    set _FLASH_SIZE 0
}

# SWJ scan chain
if { [info exists CPUTAPID] } {
   set _CPUTAPID $CPUTAPID
} else {
   # SW-DP tap id (Cortex-M4 r0p1)
   set _CPUTAPID 0x2ba01477
}

swj_newdap $_CHIPNAME cpu -irlen 4 -ircapture 0x1 -irmask 0xf -expected-id $_CPUTAPID
dap create $_CHIPNAME.dap -chain-position $_CHIPNAME.cpu

set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME cortex_m -dap $_CHIPNAME.dap

$_TARGETNAME configure -work-area-phys 0x20000000 -work-area-size $_WORKAREASIZE -work-area-backup 0

# GD32F3x0 flash controller is STM32F1-compatible (FMC_WS/KEY/OBKEY/STAT/CTL/ADDR...)
set _FLASHNAME $_CHIPNAME.flash
flash bank $_FLASHNAME stm32f1x 0x08000000 $_FLASH_SIZE 0 0 $_TARGETNAME

# SWD speed (may be updated to higher value in board config file)
adapter speed 1000

reset_config srst_nogate

if {![using_hla]} {
    # no dedicated nSRST on most CMSIS-DAP probes, use SYSRESETREQ
    cortex_m reset_config sysresetreq
}

$_TARGETNAME configure -event examine-end {
    # Keep debugger connection during low-power modes and stop watchdog
    # counters while halted.
    # DBG_CTL0 |= SLP_HOLD | DSLP_HOLD | STB_HOLD | FWDGT_HOLD | WWDGT_HOLD
    # (DBG peripheral base is 0xE0042000, DBG_CTL0 at +0x04)
    mmw 0xE0042004 0x00000307 0
}
